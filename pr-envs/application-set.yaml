apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: playground-app-preview
spec:
  generators:
    - pullRequest:
        github:
          owner: dfal
          repo: playground-app
          tokenRef:
            key: token
            secretName: github-token
        requeueAfterSeconds: 60
  template:
    metadata:
      annotations:
        argocd-image-updater.argoproj.io/write-back-method: argocd
        argocd-image-updater.argoproj.io/image-list: api=dfal/playground-app-web-api, front=dfal/playground-app-web-ui
        argocd-image-updater.argoproj.io/api.update-strategy: latest
        argocd-image-updater.argoproj.io/api.allow-tags: regexp:^{{branch}}-v.*$
        argocd-image-updater.argoproj.io/front.update-strategy: latest
        argocd-image-updater.argoproj.io/front.allow-tags: regexp:^{{branch}}-v.*$
        argocd-image-updater.argoproj.io/api.helm.image-name: api.image
        argocd-image-updater.argoproj.io/api.helm.image-tag: api.tag
        argocd-image-updater.argoproj.io/front.helm.image-name: front.image
        argocd-image-updater.argoproj.io/front.helm.image-tag: front.tag
      name: "playground-app-{{branch}}-{{number}}"
    spec:
      destination:
        namespace: "pr-{{branch}}"
        server: "https://kubernetes.default.svc"
      project: default
      syncPolicy:
        automated: {}
      source:
        path: pr-envs
        repoURL: https://github.com/dfal/playground-deployment
        targetRevision: HEAD
        helm:
          parameters:
            - name: namespace
              value: "pr-{{branch}}"
            - name: version
              value: "{{branch}}"
